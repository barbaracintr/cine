import styles from './NotFound.module.css';

function NotFound() {
    return (
        <section className={styles.container}>
            <h2>Ops...! Conteudo não encontrado.</h2>
        </section>
    )
}

export default NotFound;